### 환경변수 초기화
```bash
export $(grep -v '^#' .env | xargs)
```

### 원하는 테스트 실행
```bash
k6 run \
  --tag test=(smoke, load, stress 등) \
  --out influxdb=https://influxdb.bssm-dev.com \
  (smoke, load, stress)-test.js
  ```